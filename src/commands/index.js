const packageName = require("./package-name");
const defaultAllow = require("./default-allow");
const allows = require("./allows");
const imports = require("./imports");

module.exports = {
  packageName,
  imports,
  defaultAllow,
  allows,
};
